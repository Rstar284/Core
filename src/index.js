/* index.js */

// Imports the functions
import { readFileSync } from "fs";
import { tokenzier } from "./lib/tokenizer.js";

// Export the 'run' function
export function run(sourceFile = "") {
    //...
};